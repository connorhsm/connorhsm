[project]
name = "spawncodes"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fnvhash",
    "numpy>=2.1.3",
    "randomgen>=2.1.1",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.3.3",
]

[tool.uv.sources]
fnvhash = { git = "https://github.com/znerol/py-fnvhash.git" }
